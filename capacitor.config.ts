import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elianribeiro.musicplayer',
  appName: 'musicplayer',
  webDir: 'www'
};

export default config;
